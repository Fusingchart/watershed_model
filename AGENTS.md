# Working agreement

Work in /Users/rishabh/watershed_model.

The user requires a Git commit and push after every small completed change. Keep commits focused. Do not accumulate unrelated edits. The remote is now https://github.com/Fusingchart/watershed_model.git. Commit and push each small completed change to origin. Report push failures rather than claiming success.

Do not commit the local Python environment, caches, or original data exports containing access codes and personal information.

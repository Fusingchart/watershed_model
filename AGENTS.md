# Working agreement

Work in /Users/rishabh/watershed_model.

The user requires a Git commit and push after every small completed change. Keep commits focused. Do not accumulate unrelated edits. If no remote is configured or a push fails, report the blocker and pause further changes until resolved.

Do not commit the local Python environment, caches, or original data exports containing access codes and personal information.
